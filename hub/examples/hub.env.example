# Public-safe example only.

# Local host override for the hub runtime bind address.
# Create /etc/node-control/hub.env on the VPS from this template and set a VPN-facing IP there.
HTTP_BIND_ADDR=127.0.0.1
HTTP_PORT=18080
